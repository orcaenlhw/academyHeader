import { GowildDirective } from './gowild.directive';

describe('GowildDirective', () => {
  it('should create an instance', () => {
    const directive = new GowildDirective();
    expect(directive).toBeTruthy();
  });
});
